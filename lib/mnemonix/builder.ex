defmodule Mnemonix.Builder do
  @moduledoc false

  # defmacro __using__([]) do
  #   __using__([singleton: false])
  # end
  #
  # defmacro __using__([singleton: singleton]) do
  #   quote do
  #     @before_compile
  # end

end
